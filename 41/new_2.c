#include <stdio.h>
#include <string.h>

void parser(char array[3443])
{
    puts(array);

    // printf("size of array\n", sizeof(array));
    // for (int i = 0; i < count; i++)
    // {
    //     /* code */
    // }
}

int main()
{

    char str[] = " Aksh ";
    printf("strlen %d", strlen(str));

    return 0;
}